/// <reference types="vitest" />

import { defineConfig } from 'vitest/config';

// You can import individual configurations as well if you want to

// The base config
import vitestBaseConfig from '@pam/vitest-config/vitestBaseConfig';

// The react testing library config
import vitestReactTestingLibraryConfig from '@pam/vitest-config/vitestReactTestingLibraryConfig';

// We recommend you inspect them before using them
